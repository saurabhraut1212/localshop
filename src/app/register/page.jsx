import RegisterPage from '../../pages-section/register';

export const metadata = {
	title: 'Register Page',
	description: 'Project created for localShop',
};

const Home = () => {
	return <RegisterPage />;
};

export default Home;

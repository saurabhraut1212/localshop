import AddItemPage from '../../pages-section/add-item';

export const metadata = {
	title: 'Add Item Page',
	description: 'Project created for localShop',
};

const Home = () => {
	return <AddItemPage />;
};

export default Home;

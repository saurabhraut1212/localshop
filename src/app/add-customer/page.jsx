import AddCustomerPage from '../../pages-section/add-customer';

export const metadata = {
	title: 'Add Customer to LocalShop',
	description: 'Project created for localShop',
};

const Home = () => {
	return <AddCustomerPage />;
};

export default Home;

import GetItemsPage from '../../pages-section/get-items';

export const metadata = {
	title: 'All Items Data',
	description: 'Project created for localShop',
};

const Home = () => {
	return <GetItemsPage />;
};

export default Home;

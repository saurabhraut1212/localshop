import LoginPage from '../../pages-section/login';

export const metadata = {
	title: 'Login To The LocalShop App',
	description: 'Project created for localShop',
};

const Home = () => {
	return <LoginPage />;
};

export default Home;

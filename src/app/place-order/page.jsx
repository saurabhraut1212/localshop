import PlaceOrderPage from '../../pages-section/place-order';

export const metadata = {
	title: 'Place Order Page',
	description: 'Project created for localShop',
};

const Home = () => {
	return <PlaceOrderPage />;
};

export default Home;

import UpdateQuantityPage from '../../pages-section/update-quantity';

export const metadata = {
	title: 'Update quantity page',
	description: 'Project created for localShop',
};

const Home = () => {
	return <UpdateQuantityPage />;
};

export default Home;

import GetRemainingPage from '../../pages-section/get-remaining';

export const metadata = {
	title: 'Remaining Payment Page',
	description: 'Project created for localShop',
};

const Home = () => {
	return <GetRemainingPage />;
};

export default Home;
